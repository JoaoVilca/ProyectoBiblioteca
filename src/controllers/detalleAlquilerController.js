const { DetalleAlquiler } = require("../config/sequelize");

const registrarDetalleAlquiler = async(req, res) => {
    // try{
    //     const detalle = await DetalleAlquiler.create(req.body)

    // }
    
};
module.exports = {
  registrarDetalleAlquiler,
};
